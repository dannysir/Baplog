# React Native / Expo 호환을 위해 hoisted 레이아웃 사용.
# pnpm 격리 모드는 RN의 babel/metro helper 해상도와 자주 충돌하므로,
# 모노레포 표준 관례인 hoisted로 통일한다.
node-linker=hoisted
shamefully-hoist=true
auto-install-peers=true
strict-peer-dependencies=false
